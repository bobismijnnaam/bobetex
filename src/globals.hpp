#ifndef GLOBALS_HPP
#define GLOBALS_HPP

#include <GL/glew.h>
#include <SDL.h>
#include <SDL_opengl.h>

extern SDL_Window *window;
extern SDL_GLContext context;

extern int SCR_W;
extern int SCR_H;

extern float clearColor[3];

#endif
